'use client';

import { useEffect, useMemo, useState } from 'react';
import '../predictions.css';

const LOCAL_FIXTURES = [
  ['Fri, 11 Sep','19:45','Rennes','Marseille'],['Sat, 12 Sep','15:00','Chelsea','Hull City'],['Sat, 12 Sep','20:00','Sunderland AFC','Arsenal'],['Sat, 12 Sep','14:30','Augsburg','Bayer Leverkusen'],['Sat, 12 Sep','14:30','Borussia Dortmund','Paderborn'],['Sat, 12 Sep','14:30','Freiburg','Borussia Monchengladbach'],['Sat, 12 Sep','16:30','1. FC Cologne','Werder Bremen'],['Sat, 12 Sep','16:00','Atalanta','Cagliari'],['Sat, 12 Sep','15:15','Strasbourg','Monaco'],['Sat, 12 Sep','19:00','Paris FC','Lyon'],['Sun, 13 Sep','14:00','Coventry City','Brighton'],['Sun, 13 Sep','16:30','Man Utd','Man City'],['Sun, 13 Sep','15:00','Celta','Malaga CF'],['Sun, 13 Sep','17:15','Getafe','RC Deportivo De La Coruna'],['Sun, 13 Sep','19:45','Lazio','AC Milan'],['Sun, 13 Sep','17:00','Napoli','Bologna'],['Sun, 13 Sep','17:00','Torino','Roma'],
].map(([dateLabel,time,home,away],i)=>({id:`local-${i+1}`,dateLabel,time,home,away}));

export default function WeekendPredictionsPage() {
  const [data,setData]=useState({matches:[],futureMatches:[],recentReview:[],generatedAt:null});
  const [loading,setLoading]=useState(true); const [search,setSearch]=useState(''); const [filter,setFilter]=useState('All'); const [futureSearch,setFutureSearch]=useState('');

  useEffect(()=>{fetch('/api/weekend',{cache:'no-store'}).then(r=>r.ok?r.json():Promise.reject()).then(setData).catch(()=>{}).finally(()=>setLoading(false));},[]);

  const fallback=useMemo(()=>LOCAL_FIXTURES.map(m=>({...m,signal:'Cautious',prediction:'Pattern review',tracked:true,lastResult:null,source:'MatchPattern historical evidence',evidence:{sample:0}})),[]);
  const boardMatches=data.matches?.length?data.matches:(data.futureMatches||[]).filter(m=>m.tracked).length?(data.futureMatches||[]).filter(m=>m.tracked):fallback;
  const futureSource=data.futureMatches?.length?data.futureMatches:LOCAL_FIXTURES;
  const filtered=useMemo(()=>boardMatches.filter(m=>`${m.home} ${m.away}`.toLowerCase().includes(search.toLowerCase())).filter(m=>filter==='All'||m.signal===filter),[boardMatches,search,filter]);
  const futureFiltered=useMemo(()=>futureSource.filter(m=>`${m.home} ${m.away}`.toLowerCase().includes(futureSearch.toLowerCase())),[futureSource,futureSearch]);
  const strong=boardMatches.filter(m=>m.signal==='Strong').length;

  return <main className="predictor-shell weekend-pro">
    <header className="predictor-header"><a className="brand" href="/predictions/weekend">Match<span>Pattern</span></a><nav><a className="active" href="/predictions/weekend">Next Weekend</a><a href="/lex-prediction">Lex&apos;s Prediction</a><a href="/predictions">History</a></nav><div className="header-pill">LIVE BOARD ⚽</div></header>
    <section className="hero pro-hero"><div className="hero-copy"><div className="eyebrow">MATCHPATTERN • WEEKEND INTELLIGENCE</div><h1>Next weekend.<br/><span>Read the pattern.</span></h1><p>Fresh fixtures are combined with the concluded weekend, the MatchPattern history library and the latest available result for each tracked club. The goal is a transparent signal — not a promise.</p><div className="hero-actions"><a href="#future" className="primary-btn">View future matches ↓</a><a href="/lex-prediction" className="secondary-btn hero-secondary">Open Lex&apos;s engine →</a></div><div className="live-line"><span className="live-dot"/> DATA FEED ACTIVE <span>•</span> {loading?'Refreshing fixtures…':`${futureFiltered.length} future fixtures loaded`}</div></div><div className="hero-card intelligence-card"><div className="card-top"><span>WEEKEND SIGNAL</span><strong>{strong} STRONG</strong></div><div className="big-number">{boardMatches.length}</div><small>tracked fixtures</small><div className="mini-grid"><div><b>{futureFiltered.length}</b><span>future matches</span></div><div><b>{data.recentReview?.length||0}</b><span>recent results</span></div></div></div></section>
    <section id="future" className="future-section pro-section"><div className="section-heading"><div><div className="eyebrow">UPCOMING FIXTURES • LIVE DATA</div><h2>Next future matches</h2><p className="section-subtitle">Every upcoming fixture found in the connected leagues. Tracked matches are marked for Lex analysis.</p></div><span className="data-note">{futureFiltered.length} matches</span></div><div className="future-toolbar"><input value={futureSearch} onChange={e=>setFutureSearch(e.target.value)} placeholder="Search any club…" aria-label="Search future matches"/><span>Auto-updated from the football feed</span></div><div className="future-list">{futureFiltered.map((m,i)=><article className={`future-match ${m.tracked?'tracked':''}`} key={m.id||i}><div className="future-index">{String(i+1).padStart(2,'0')}</div><div className="future-date"><b>{m.dateLabel}</b><span>{m.time} WAT</span></div><div className="future-teams"><strong>{m.home}</strong><span>vs</span><strong>{m.away}</strong></div><div className="future-status">{m.tracked?<><span className={`signal ${(m.signal||'Cautious').toLowerCase()}`}>{m.signal||'Cautious'}</span><small>LEX ANALYSIS READY</small></>:<small>FIXTURE</small>}</div></article>)}</div>{!futureFiltered.length&&<div className="empty">No future fixture matches your search.</div>}</section>
    <section className="review-strip"><div className="review-title"><div className="eyebrow">LAST WEEKEND</div><h2>What just happened</h2><p>Recent results now feed the next-match signal.</p></div><div className="review-scroll">{(data.recentReview||[]).slice(0,8).map(m=><div className="review-card" key={m.id}><span>{m.dateLabel}</span><b>{m.home}</b><strong>{m.result}</strong><b>{m.away}</b></div>)}</div></section>
    <section id="weekend" className="matches-section pro-section"><div className="section-heading"><div><div className="eyebrow">NEXT WEEKEND • 11–13 SEP</div><h2>Lex prediction board</h2></div><span className="data-note">{data.generatedAt?`Updated ${new Date(data.generatedAt).toLocaleString('en-GB',{dateStyle:'short',timeStyle:'short'})}`:'Loading…'}</span></div><div className="board-toolbar"><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search a club…" aria-label="Search club"/><div className="filter-pills">{['All','Strong','Balanced','Cautious'].map(o=><button key={o} className={filter===o?'selected':''} onClick={()=>setFilter(o)}>{o}</button>)}</div></div><div className="prediction-cards">{filtered.map((m,i)=><article className="match-card" key={m.id||i}><div className="match-top"><span>#{String(i+1).padStart(2,'0')} • {m.dateLabel} {m.time}</span><span className={`signal ${(m.signal||'Cautious').toLowerCase()}`}>{m.signal||'Cautious'}</span></div><div className="match-main"><div><b>{m.home}</b><small>HOME</small></div><div className="versus">VS</div><div className="away"><b>{m.away}</b><small>AWAY</small></div></div><div className="pick-box"><span>LEX / PATTERN PICK</span><strong>{m.prediction||'Pattern review'}</strong></div><div className="match-foot"><span>Latest signal: {m.lastResult||'No recent result'}</span><span>{m.source||'MatchPattern historical evidence'}</span></div></article>)}</div>{!filtered.length&&<div className="empty">No tracked fixture matches your filters.</div>}</section>
    <section className="method pro-method"><div><div className="eyebrow">THE ENGINE</div><h2>Results → patterns → next fixtures.</h2></div><div className="method-grid"><p><b>01 • REVIEW</b><br/>The concluded weekend is captured and shown openly.</p><p><b>02 • COMPARE</b><br/>Recent results are compared with the historical market pattern.</p><p><b>03 • SIGNAL</b><br/>Each fixture gets a Strong, Balanced or Cautious signal.</p></div></section>
    <footer><b>MatchPattern</b><span>Lex&apos;s Prediction Engine</span><span>•</span><span>Analysis and entertainment only. No result is guaranteed.</span></footer>
  </main>;
}
