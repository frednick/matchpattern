export const metadata={title:'MatchPattern | Prediction History',description:'Historical football prediction data and results.'};
export default function PredictionsLayout({children}){return children}
