'use client';
import {useMemo,useState} from 'react';
import './predictions.css';
import {HISTORICAL_27} from '../data/historical27';
import {RECENT_29} from '../data/recent29';

const originalMatches=HISTORICAL_27.map((x,i)=>({...x,id:i+1}));
const latestMatches=RECENT_29;

function MatchTable({items}){const [search,setSearch]=useState('');const filtered=useMemo(()=>items.filter(m=>`${m.home} ${m.away}`.toLowerCase().includes(search.toLowerCase())),[items,search]);return <><div className="filters"><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search club..." aria-label="Search club"/></div><div className="table-wrap"><table><thead><tr><th>#</th><th>Match</th><th>Actual result</th><th>Prediction</th><th>Odds</th><th>Status</th></tr></thead><tbody>{filtered.map(m=><tr key={m.id}><td className="muted">{m.id}</td><td><div className="teams"><b>{m.home}</b><span>vs</span><b>{m.away}</b></div></td><td className="score">{m.score}</td><td><span className="pick">{m.pick}</span></td><td><strong>@{m.odds}</strong></td><td><span className={m.status==='Lost'?'lost':m.status==='Not Started'?'pending':'won'}>{m.status}{m.status==='Won'?' ✓':''}</span></td></tr>)}</tbody></table>{!filtered.length&&<div className="empty">No club found.</div>}</div></>}

export default function PredictionsPage(){const completed=latestMatches.filter(m=>m.status!=='Not Started');const wins=completed.filter(m=>m.status==='Won').length;const latestRate=((wins/completed.length)*100).toFixed(1);return <main className="predictor-shell"><header className="predictor-header"><a className="brand" href="/predictions/weekend">Match<span>Pattern</span></a><nav><a href="/predictions/weekend">Next Weekend</a><a className="active" href="/predictions">History</a></nav><div className="header-pill">27 / 27 WON • LATEST {wins}/{completed.length}</div></header><section className="hero"><div className="hero-copy"><div className="eyebrow">MATCH HISTORY • 30 AUG → 07 SEP 2026</div><h1>Past picks.<br/><span>Real results.</span></h1><p>MatchPattern keeps the original 27-match foundation exactly as supplied, then adds the latest 29 supplied fixtures as a separate results layer. Nothing from the original dataset is replaced or rewritten.</p><div className="hero-actions"><a href="/predictions/weekend" className="primary-btn">Open next weekend →</a><span className="disclaimer">Analysis & entertainment only.</span></div></div><div className="hero-card"><div className="card-top"><span>ORIGINAL 27</span><strong>27 / 27 WON</strong></div><div className="ring"><span>{latestRate}%</span><small>latest completed hit rate</small></div><div className="hero-stat"><b>{latestMatches.length}</b><span>latest supplied fixtures</span></div></div></section><section className="matches-section"><div className="section-heading"><div><div className="eyebrow">FOUNDATION DATA</div><h2>Original 27</h2></div><span className="data-note">Preserved exactly as supplied</span></div><MatchTable items={originalMatches}/></section><section className="matches-section"><div className="section-heading"><div><div className="eyebrow">LATEST RESULTS</div><h2>5–7 September results</h2></div><span className="data-note">29 supplied fixtures • 27 completed • 2 not started</span></div><MatchTable items={latestMatches}/></section><section className="method"><div><div className="eyebrow">MATCHPATTERN DNA</div><h2>Two layers of evidence.</h2></div><p>The original 27 remain the foundation. The latest supplied results are stored separately so the system can compare recurring clubs, markets, odds and outcomes over time without altering the original source record. Past performance does not guarantee future outcomes.</p></section><footer><b>MatchPattern</b><span>Independent football match analysis</span><span>•</span><span>No result is guaranteed.</span></footer></main>}
