'use client';
import {useMemo,useState} from 'react';
import './predictions.css';
import {HISTORICAL_27} from '../data/historical27';

const matches=HISTORICAL_27.map((x,i)=>({...x,id:i+1}));

export default function PredictionsPage(){const [search,setSearch]=useState('');const filtered=useMemo(()=>matches.filter(m=>`${m.home} ${m.away}`.toLowerCase().includes(search.toLowerCase())),[search]);return <main className="predictor-shell"><header className="predictor-header"><a className="brand" href="/predictions/weekend">Match<span>Pattern</span></a><nav><a href="/predictions/weekend">Next Weekend</a><a className="active" href="/predictions">History</a></nav><div className="header-pill">27 / 27 WON ✓</div></header><section className="hero"><div className="hero-copy"><div className="eyebrow">HISTORICAL BOARD • ORIGINAL 27</div><h1>Past picks.<br/><span>Clear results.</span></h1><p>A transparent record of the original 27 supplied matches, including the prediction, odds and actual score. These results are stored as MatchPattern historical evidence for future pattern analysis.</p><div className="hero-actions"><a href="/predictions/weekend" className="primary-btn">Open next weekend →</a><span className="disclaimer">Analysis & entertainment only.</span></div></div><div className="hero-card"><div className="card-top"><span>RECORDED RESULTS</span><strong>27 / 27 WON</strong></div><div className="ring"><span>100%</span><small>historical hit rate</small></div><div className="hero-stat"><b>{(matches.reduce((s,m)=>s+Number(m.odds),0)/matches.length).toFixed(2)}</b><span>average recorded odds</span></div></div></section><section className="matches-section"><div className="section-heading"><div><div className="eyebrow">MATCH CENTER</div><h2>Prediction history</h2></div><span className="data-note">27 original results • stored in project data</span></div><div className="filters"><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search club..." aria-label="Search club"/></div><div className="table-wrap"><table><thead><tr><th>#</th><th>Match</th><th>Actual result</th><th>Prediction</th><th>Odds</th><th>Status</th></tr></thead><tbody>{filtered.map(m=><tr key={m.id}><td className="muted">{m.id}</td><td><div className="teams"><b>{m.home}</b><span>vs</span><b>{m.away}</b></div></td><td className="score">{m.score}</td><td><span className="pick">{m.pick}</span></td><td><strong>@{m.odds}</strong></td><td><span className="won">{m.status} ✓</span></td></tr>)}</tbody></table>{!filtered.length&&<div className="empty">No club found.</div>}</div></section><section className="method"><div><div className="eyebrow">MATCHPATTERN DNA</div><h2>Results now feed the history.</h2></div><p>The original 27 matches are now stored as structured historical data rather than being hard-coded only into the screen. This gives MatchPattern a reusable record of picks, odds and actual scores for future pattern analysis.</p></section><footer><b>MatchPattern</b><span>Independent football match analysis</span><span>•</span><span>No result is guaranteed.</span></footer></main>}
